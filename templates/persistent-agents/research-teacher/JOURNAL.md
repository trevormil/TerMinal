# JOURNAL — research-teacher

Append-only run history. One entry per run, newest last.

## Seeded

Agent created from `templates/persistent-agents/research-teacher/`. No runs yet.

Reminder for whoever wires the schedule: **`engine: 'claude'` is required.**
Scheduled runs default to `codex exec`, which has no web search, and this agent
must never teach from memory.
