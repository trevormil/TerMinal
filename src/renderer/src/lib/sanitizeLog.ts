// Moved to src/shared/run-log/sanitize.ts so the shared run-log parser can use
// it too; re-exported here so existing renderer imports keep working.
export { sanitizeLog } from '../../../shared/run-log/sanitize'
