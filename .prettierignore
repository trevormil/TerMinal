node_modules
out
dist
*.log
landing
bun.lock
# git submodules — not ours to format
vendor
templates
# runtime state (tickets, schedules, agent state) — not source
.TerMinal
# extensionless TS bin scripts — prettier can't infer the parser
bin
# generated verbatim from src/main/repo-state-inline.ts; every copy of the block
# must stay byte-identical, so no formatter may touch it
src/runner/repo-state-block.js
# prose docs are hand-maintained (anchored headings, tables) — not prettier-owned
*.md
**/*.md

# Xcode owns the asset catalog and rewrites Contents.json in its own format.
ios/**/*.xcassets
# Generated by xcodegen.
ios/*.xcodeproj
# Xcode build output (device/simulator archives, intermediates).
ios/build

# Generated by `bun run lint:baseline` — eslint owns its formatting.
eslint-suppressions.json
